apiVersion: v1
kind: Service
metadata:
  name: door-sensor-service
  labels:
    app: door-sensor-monitor
spec:
  type: ClusterIP
  selector:
    app: door-sensor-monitor
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
